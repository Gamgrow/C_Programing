#include <stdio.h>

int main()
{
    // int type
    // int a = 5;
    // int *ptr = &a;
    // ptr = ptr + 5;
    // printf("%u", ptr);

    // float type
    // float f = 5.5;
    // float *ptr = &f;
    // ptr++;
    // printf("%u", ptr);

    // char type
    // char ch = 'a';
    // char *ptr = &ch;
    // // ptr++;
    // printf("%u", ptr);

    // double 
    double d=5;
    double *ptr=&d;
    ptr++;
    printf("%u",ptr);

    return 0;
}
